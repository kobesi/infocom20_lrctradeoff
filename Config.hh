#ifndef _CONFIG_H_H_H_
#define _CONFIG_H_H_H_

#include <iostream>
#include <fstream>
#include <sstream>
#include <string>

using namespace std;

class Config{
  public:
    void read_config(void);
}; 

#endif
